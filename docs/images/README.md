# Screenshots

| File | What it shows |
|---|---|
| `card-back.png` | The back of *el embarazo*, the example with the most fields filled. |
| `terminal.png` | One run of `anki-vocab maleta`, start to finish. |
| `card-front.png` | The front of *la maleta*, cropped to the card itself. |
| `card-back-full.png` | The back of the same card, so the pair reads front-then-back. |

Anki's own window in dark mode. To retake one, study the card and screenshot the
window (⇧⌘4 then space); crop the dead space below the content for fronts.
